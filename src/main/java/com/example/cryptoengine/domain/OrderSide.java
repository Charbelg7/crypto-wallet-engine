package com.example.cryptoengine.domain;

/**
 * Order side enumeration (buy or sell).
 */
public enum OrderSide {
    BUY,   // Buy order: user wants to acquire base currency
    SELL   // Sell order: user wants to sell base currency
}
