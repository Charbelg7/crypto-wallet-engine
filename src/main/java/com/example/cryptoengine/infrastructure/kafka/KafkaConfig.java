package com.example.cryptoengine.infrastructure.kafka;

import org.springframework.context.annotation.Configuration;

/**
 * Kafka configuration.
 * Spring Boot auto-configuration handles most of the setup via application.properties.
 * This class can be used for additional custom configuration if needed.
 */
@Configuration
public class KafkaConfig {
    // Spring Boot auto-configuration handles KafkaTemplate setup
    // Additional custom configuration can be added here if needed
}
